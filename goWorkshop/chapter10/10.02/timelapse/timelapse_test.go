package timelapse

import (
	"testing"
)

func testScene(t *testing.T) {

result := ElapsedTime(start, end)

expected := return "The total execution time elapsed is: " + hours + "hours and " + minutes + "minutes, " + seconds + "seconds!"



}
